spring:
  application:
    name: 'service-aggregator'
  profiles:
    active: 'local'
springdoc:
  swagger-ui:
    path: '/service-aggregator/swagger'
    disable-swagger-default-url: true
    operations-sorter: 'alpha'
    tags-sorter: 'alpha'
